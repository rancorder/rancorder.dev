# AI Operator Mode

The repository accepts authorized `/ai ...` commands on GitHub Issue or PR conversations. Gemini CLI implements the request on an isolated branch, repository validation runs before push, and the change must be reviewed through a pull request before merge.

The operator is governed by `AGENTS.md` and `GEMINI.md`.
